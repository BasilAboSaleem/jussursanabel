<% if (user && user.role !== 'admin' && user.role !== 'super_admin') { %>
<div id="support-widget" class="nm-support-widget">
    <a href="/support/chat" class="nm-support-widget__btn" aria-label="<%= __('support_widget_label') %>">
        <i class="fas fa-headset"></i>
        <span class="nm-support-widget__badge" id="support-badge" style="display: none;">0</span>
    </a>
</div>
<% } %>
